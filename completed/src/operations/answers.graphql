mutation submitAnswer(
  $gameId: ID!
  $playerId: ID!
  $questionId: ID!
  $answer: String!
) {
  submitAnswer(
    gameId: $gameId
    playerId: $playerId
    questionId: $questionId
    answer: $answer
  ) {
    id
  }
}
